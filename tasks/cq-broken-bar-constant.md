# Extract broken-bar character into a named constant

The broken-bar character `'\u{00A6}'` is used in `rcd.rs` at multiple locations without explanation. Extract it into a named constant with a comment explaining its purpose (VB6 separator character).
